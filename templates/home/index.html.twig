{% extends 'base.html.twig' %}

{% block title %}Accueil - EcoGuide
{% endblock %}

{% block stylesheets %}
	{# {{ parent() }} #}
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
	<style>
		/* Ваши стили остаются без изменений */
		.container {
			/* display: grid; */
			/* grid-template-columns: 1fr 1fr 1fr 1fr 1fr;
			gap: 20px;
			background-color: transparent; */
		}

		/* .carousel-inner {
			display: flex;
			justify-content: space-between;
			position: relative;
			height: 100vh;
			overflow: hidden;
		}

		.carousel-item {
			cursor: pointer;
			opacity: 0.8;
			transition: opacity 0.3s, transform 0.3s;
		}

		/* Поднимаем карточку при наведении */
		/* .carousel-item:hover {
			opacity: 1;
			transform: translateY(-10px);
		} */

		/* Добавляем стили для каждой секции */
		.latest-news,
		.featured-projects,
		.utilization,
		.community,
		.projects {
			flex: 1;
			padding: 20px;
			background-color: #d6fda199;
			border-radius: 10px;
			transition: transform 0.3s;
		}

		/* Перемещаем секцию вверх при фокусе */
		.latest-news:focus,
		.featured-projects:focus,
		.utilization:focus,
		.community:focus,
		.projects:focus {
			z-index: 2;
			transform: translateY(-50px);
		}
		#test {
			display: flex;
			flex-direction: column
		}
	</style>
{% endblock %}

{% block body %}

</div>
<div
	class="home">


	<!-- Hero Banner -->
	<div class="banner-section">
		<img src="{{ asset('assets/images/banner-home.png') }}" alt="Nature Banner" width="100%" height="auto">
		<div class="banner-content">
			<h1>Guidons le changement, préservons notre environnement</h1>
		</div>
	</div>


	<section
		id="cards" class="container-fluid row justify-content-center gap-5">
		{# Latest news #}
		<div class="card-custom col-9 col-lg-5">
			<h2>Dernières actualités</h2>
			{% for article in latest_articles %}
				<article>
					<h3>{{ article.getTitle() }}</h3>
					{# <p>{{ post.getSummary() }}</p> #}
					<a href="{{ path('app_blog') }}">Lire la suite</a>
				</article>
			{% endfor %}


		</div>


		{# Projects  #}
		<div class="card-custom col-9 col-lg-5">
			<h2>Projets</h2>
			{% for project in featured_projects %}
				<article>
					{# <h3>{{ project.getName() }}</h3> #}
					<h3>
						<a href="{{ path('app_projects', {'id': project.getId()}) }}">{{ project.getName() }}</a>
					</h3>

					<!-- Добавлен блок для отображения только названия проекта -->
					{# <p>{{ project.getDescription()|raw }}</p>
																																																																										                <a href="{{ path('app_projects', {'id': project.getId()}) }}">Détails du projet</a> #}
				</article>
			{% endfor %}
		</div>


		{# Tri #}
		<div class="card-custom col-9 col-lg-3">
			<h2>Tri et Recyclage</h2>
			<p>Titre et description de la section.</p>
			<article>
				<h3>Instructions pour le tri des déchets</h3>
				<!-- Добавьте секции с инструкциями, графическими иллюстрациями и анимациями -->
			</article>
			<article>
				<h3>Conseils utiles</h3>
				<!-- Добавьте блок с полезными советами -->
			</article>

		</div>


		{# Forum #}
		<div class="card-custom col-9 col-lg-4">
			<h2>Forum</h2>
			{% for post in latest_posts %}
				<article>
					<h3>{{ post.getTitle() }}</h3>
					{# <p>{{ post.getSummary() }}</p> #}
					<a href="{{ path('app_forum_show_post_details', {'id': post.getId()}) }}">Lire la suite</a>
				</article>
			{% endfor %}

		</div>


		{# Contact #}
		<div class="card-custom col-9 col-lg-3">
			<h2>Contact</h2>
			<article>
				<h3>Email:<br>
					info@ecoguide.com
					<br>
					<br>
					Téléphone:<br>
					+33 123 456 789</h3>
			</article>
		</div>

	</section>

</div>
<div class="container" id="test">
{% endblock %}
